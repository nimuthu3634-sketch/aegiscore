# Docker Notes

This folder holds local container support files for AegisCore.

Current contents:
- `postgres/init/`: startup SQL hooks for local PostgreSQL containers

Keep Docker-specific files here when the project grows, such as custom entrypoints or service configs.
